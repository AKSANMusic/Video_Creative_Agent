"""Narrative sequencer subsystem (Stage 3).

Consumes Stage-1 image metadata + Stage-2 audio map and produces a
frame-accurate ``timeline.json`` describing which image is shown when, with
which transition, beat-locked to audio downbeats.
"""
